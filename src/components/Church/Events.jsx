import { Typography } from "@mui/material"

function Events() {
  return (
    <div>
      <Typography variant="h6" sx={{ color: 'GrayText' }}>Events</Typography>
    </div>
  )
}

export default Events