import { Typography } from "@mui/material"

function Donations() {
  return (
        <div>
      <Typography variant="h6" sx={{ color: 'GrayText' }}>Donations</Typography>
    </div>
  )
}

export default Donations